# EPForecast Methodology — Python dependencies
# Tested on Python 3.12

# Core data
numpy>=1.26,<2.0
pandas>=2.1,<3.0

# Machine learning (gradient boosting ensemble)
scikit-learn>=1.4,<2.0
lightgbm>=4.3,<5.0
xgboost>=2.0,<3.0
joblib>=1.3,<2.0

# LSTM encoder (v10.0+) — optional, only needed if using LSTM embeddings
torch>=2.2,<3.0

# Utilities
holidays>=0.46,<1.0
